# Global Android 1.0.1 protocol subset

Shared by TW/HK/MO, EN Region and Korea. Includes only anonymous Version and GetServerList,
with distinct generated native codecs and a family-aware fingerprint. No JP player schemas,
SDK identity secrets, login implementation or unused game protocol inventory are included.
See `docs/REGIONS.md` for the current capability boundary.
