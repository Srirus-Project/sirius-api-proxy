# JSON is valid YAML. default_cdn_root is the first CDN line of the EN Region server-list entry.
# Optional Master pipeline (docs/REGIONS.md#master-data): remove the leading "# " from the
# commented lines to download Master data without CDN authorization and publish it to Git.
# Optional resource snapshots for the asset updater (docs/REGIONS.md#resource-snapshots): the
# resource_snapshot line serves schema-3 snapshots from the anonymous catalog .hash.
# Optional Global SDK guest account (docs/ACCOUNTS.md#global-accounts): uncomment the accounts
# and global_login lines, create the private identity file and export SIRIUS_GLOBAL_SDK_APP_KEY.
{
  "region": "en",
  "platform": "Android",
  "listen": "127.0.0.1:9999",
  "environment": "release",
  "endpoint": "https://l14-prod-va-all-gs-sirius.bilibiligame.net",
  "client_version": "1.0.1",
  "session_lock": true,
  "protocol_directory": "protocol/global/1.0.1",
  "api_token_env": "SIRIUS_EN_API_TOKEN",
  "internal_token_env": "SIRIUS_EN_INTERNAL_TOKEN",
  "default_cdn_root": "https://l14-prod-sg-patch-sirius.bilibiligame.net/prod/en_3e8a72c5f1d9066b9a37c2e85f619db0",
  # "master_directory": "data/en/master",
  # "master_update": {"cdn_authorization": "none", "key_hex_env": "SIRIUS_MASTER_KEY_HEX", "iv_hex_env": "SIRIUS_MASTER_IV_HEX", "interval_seconds": 300},
  # "master_git": {"state_directory": "data/en/master-git", "layout": "indented_root", "branch": "main",
  #   "remote": {"url": "https://github.com/Srirus-Project/sirius-en-master.git", "authorization_env": "SIRIUS_MASTER_GIT_EN_AUTHORIZATION"}},
  # "resource_snapshot": {"cdn_authorization": "none", "catalog_hash_ttl_seconds": 60},
  # "accounts": [{"name": "en-guest", "global_identity_file": "/run/secrets/sirius-global-guest-1.json"}],
  # "global_login": {"sdk_app_key_env": "SIRIUS_GLOBAL_SDK_APP_KEY", "login_min_interval_seconds": 300, "max_logins_per_day": 24},
  "cdn_credential_env": {}
}
