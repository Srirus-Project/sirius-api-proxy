# Sirius 1.2.0 restoration and acceptance ledger

Objective: retain Haruki's reusable service/platform capabilities, replace all Sekai-specific logic with Sirius implementations, test the complete production pipeline on yhm01, then publish both repositories as 1.2.0. A working CLI or one successful export alone is not completion.

Reference baseline: local Haruki-Sekai-API 07da6b80e6a59ece89251f4694afe94bea72e131 and Haruki-Sekai-Asset-Updater 3d33ed037f0ef5009e361e0535b3b19f8c239947. Preserve MIT attribution. Existing Sirius release baselines: API 5ab1e390ca514e550cb609ae4d299b19c105b79e, updater e7ffb7b905958de88aa8d5a06327dafcc8acd8ce.

## API requirements (pending unless evidenced in the work log)

- Multi-region service configuration/routing with per-region protocol family, credentials and isolated state; retain v1.1 single-region config compatibility and reserved CN.
- Account pool, per-account locking, selection, health/cooldown and credential reload. No speculative retries of account mutations; no fabricated Global login.
- Configurable response cache, optional Redis backend, region/account/protocol-aware identity and invalidation; private data never shared through public cache.
- Upstream proxy, deadlines/limits, controlled retry policy, server TLS, logs/access logs and trusted-forwarding configuration.
- Remote node routing, priorities/failover, token-scoped internal calls and capability/version boundaries.
- Master registry/manifests, owner/consumer synchronization, notifications, optional generic persistence and Git publication. Do not restore Sekai table models or Ent code.
- Full documented config surface, examples, migration, meaningful local integration tests including failure/authorization cases.

## Updater requirements (all pending unless specifically evidenced)

- Authenticated long-running HTTP service, region configs, job submission/list/detail/cancel/retry, bounded queue/concurrency, progress, retention and safe shutdown/restart.
- Invoke Sirius catalog/download/verify/export pipeline from the service, not a stub or arbitrary shell executor; retain CLI support.
- Incremental download/export records by verified content identity; cache reuse, partial failure recovery and immutable publication semantics.
- Include/exclude filters, priority ordering, exact type selection and scoped replay; selected work is never claimed as full-catalog verification.
- OpenDAL/local/S3 providers, upload concurrency/retries and verified upload-before-cleanup policy; region-scoped object keys and secret references.
- Format configuration for image/audio/video and backend choice where implementable for Sirius; retain correct alpha/split audio semantics.
- Separate stage concurrency, bounded resource budgets, configurable retries/timeouts, logging/access logging and TLS/proxy configuration.
- Update trigger/scheduling, completion notification and generic publication integration where applicable. Sekai chart-hash/character-ID/3D model adapters are removed, not copied.
- End-to-end tests cover queue saturation, cancellation, restart, per-region serialization, incremental behavior, upload failure safety and secrets.

## Release gates (all pending)

- Audit each feature against original code/config; no placeholders or silent ignored config fields; record any game-specific non-applicability with evidence.
- Both repositories fmt/check/Clippy/tests and release packaging smoke pass.
- yhm01 official candidate full JP download/decrypt/export through restored service; retain and independently hash every exported file.
- yhm01 verify incremental second run, job API/auth/cancel, resource limits, restart and configured storage path; existing Haruki services unchanged.
- Global remains limited to verified contracts unless additional protocol research validates functionality; region isolation test for TW/EN/KR and CN rejection.
- Public source/artifact secret audit, three-platform artifacts, exact commits, version 1.2.0 tags/releases, published artifact audit.

## Work log

- Initial audit confirmed the missing components are real scope loss, not merely shorter examples/defaults.
- Implementation begins with the updater's durable job lifecycle and service orchestration; API restoration follows against the same ledger.

- Updater durable job ledger implemented and five lifecycle tests passed: restart interruption, exclusive state ownership, queue/retention bounds, per-region scheduling, cancellation acknowledgement, storage failure atomicity and reserved-region rejection. HTTP and pipeline integration remain pending; this does not satisfy the service gate yet.

- User requires category investigation before adapting filters. Verified Sirius catalog labels: InitialDownload, Everything, MV; use native key/dependency membership, not Sekai start_app/on_demand. Everything is a subset of full catalog (JP baseline: 13,364 versus 13,367 remote files). Exact tutorial/runtime required-address composition remains unverified; no invented preset.

- Multi-region service assembly implemented with one listener, independent regional clients/protocols/observations/locks, legacy single-region routes and per-region bearer scopes. Local four-region tests verify wrong-scope rejection, Global 501, CN/unknown route rejection and JP-only protocol reload. Updater opt-in regional routes cover version refresh and snapshot transport. Account pools, cache and the remaining API requirements are still pending.

- Account pooling restored: least-loaded selection with rotating ties, per-account session locks, stable private identity routes, transient cooldown, auth-failure quarantine and atomic credential-file reload after active calls drain. Legacy account configuration is supported. Local HTTP/2 fixtures cover concurrent independent accounts, same-account serialization, non-replay on failed auth, timeout recovery, credential activation barriers and invalid-file rollback. No Global login or registration was fabricated.

- Regional gRPC request policy now configures one end-to-end deadline, admission concurrency, wire response size and bounded anonymous-read retries. Local HTTP/2 tests verify shrinking timeout headers, exact retry count, maintenance/authenticated non-replay, queued-request deadline accounting, permit release and valid oversized protobuf rejection. Upstream proxy, server TLS, access logging and forwarding trust remain pending.

- Added bounded memory and optional Redis caching for public announcements and sanitized rankings only. Profile/deck/identity/private-data/version calls bypass caching. Keys scope region, origin, account credential digest/generation, protocol fingerprint/generation, Master version and request input; cache errors fail open within operation/request budgets. Local tests cover TTL/bounds, error exclusion, reload invalidation and isolated Redis outage/account separation. Per-endpoint TTLs, single-flight and stale-while-revalidate remain pending.

- Cache policy now supports separate announcement/event/song/challenge TTLs, per-route disablement and process-local single-flight fills with bounded lock storage. Effective TTL is included in cache identity and applied to Redis expiry. Tests verify independent expiry, disabled-route RPC behavior, cancellation-safe fill handoff and concurrent miss coalescing. Stale-while-revalidate remains pending.

- Regional outbound HTTP/HTTPS CONNECT proxies now sit beneath the existing verified origin TLS/HTTP2 stack, with separately scoped environment-referenced proxy authorization and a full connection-establishment deadline. Refused/malformed tunnels fail without direct fallback or retry; local fixtures verify RPC/trailer preservation, credential separation, reuse, proxy isolation, deadlines, DNS/IPv6 authorities and rejection of untrusted certificates at both TLS layers. fmt/check/Clippy and the default suite pass (72 passed, one optional Redis test ignored). This completes game gRPC proxy transport only; CDN transport, inbound TLS/access logs/trusted forwarding and other restoration gates remain pending.

- Optional inbound TLS is now wired into the production listener, with bounded PEM reads, certificate/key matching, Unix private-key permissions, ALPN HTTP/1.1+HTTP/2 and a configurable handshake timeout. Certificates load before bind/background workers. Existing plaintext deployment remains supported; TLS closes plaintext/stalled peers and gracefully drains active requests. API multi-region TLS belongs only at the deployment root. fmt/check/Clippy and the default suite pass (76 passed, one optional Redis test ignored). Structured/access logging and forwarding trust remain pending.

- Access logging is now configurable at the listener root: JSON/text, stdout or rotating files, bounded asynchronous queue and retention. Records contain only matched route templates, generated request IDs, peer/client IPs, outcome/status and header-response latency; credentials, query strings, bodies and route IDs are excluded. Trusted forwarding walks explicit CIDRs from the socket peer toward the first untrusted hop and rejects malformed/duplicate/oversized headers. Tests exercise redaction, spoofed chains, IPv6, cancellation, append behavior and HTTPS/HTTP2 listener integration. fmt/check/Clippy and the full default suite pass: 79 passed, one optional Redis test ignored. Application log configuration remains pending.

- Master CDN transport now has its own validated HTTP/HTTPS proxy references, connection/body timeouts, whole-update deadline and bounded transient retries under `master_update.network`. Defaults retain the previous one-attempt/10s-connect/60s-request/600s-update policy; waiting for the updater lock now consumes the same deadline. Explicit proxy authentication remains separate from CDN Basic credentials and game RPC sessions, with normal TLS verification, no redirects, no direct fallback and no ambient proxy discovery. Tests cover actual manifest/table publication through a proxy, HTTP retry counts, terminal auth/redirect/integrity failures, request timeout recovery, whole-deadline/backoff/queue bounds, invalid config before I/O and both proxy/origin TLS rejection. fmt/check/Clippy/default tests pass (86 passed, one optional Redis test ignored). Master registry/owner-consumer sync, generic persistence/publication/notifications and other platform/production gates remain pending.

- Application logging is now configured at the executable/service root: off/error/warn/info/debug/trace, text/JSON, stderr/stdout or UTC-rotating append files, bounded queue and retention. It reuses the access-log sink but filters to application targets and an explicit operational-field allowlist; dependency request payloads and raw resource/decoder details are excluded. Updater stages/retries/progress and persisted job terminal states emit structured events. Default events use stderr, preserving CLI stdout; existing CLI diagnostics remain separate. Multi-region/per-profile nested logging is rejected. Tests verify levels, forbidden fields/dependency targets, numeric JSON fields, escaped newlines, UTF-8 limits, append/flush, disabled logging and invalid sinks. fmt/check/Clippy/default tests pass (88 passed, one optional Redis test ignored). Both actual local service processes also passed JSON file output, clean stdout and SIGTERM exit/flush checks; the updater check included a persisted failed job and its terminal log event. Other restoration groups and full yhm01/three-platform release acceptance remain pending.

- Added API-side typed asset job HTTP transport for submit/poll: dedicated bearer origin, optional explicit plaintext, normal TLS verification, no ambient proxy/redirect/fallback/retry, bounded deadlines and 64 KiB streamed response cap. Validates request/job/key identity, completed outcome region/digest and publication shape; errors never expose upstream bodies or tokens. Local HTTP tests cover accepted/polled/completed jobs, wrong identities/digests, malformed/oversized results, terminal-state consistency, 302/401/404/409/429/503, timeout, reserved CN and unsafe config. fmt/check/Clippy/default suite pass (89 passed, one optional Redis test ignored). docs/ASSET_DISPATCH.md documents responsibility split and the still-pending durable owner/config/worker integration; no inert config setting is exposed. Full production/release gates remain pending.

- Added the API dispatch outbox primitive: single-owner atomic ledger, deterministic region/destination/profile-revision/catalog/scope identity, pending-before-send and first-attempt persistence, pinned acknowledged job IDs, terminal completion/failure records and explicit bounded history without silent pruning. Process restart preserves ambiguous sends and known completed/failed identities; failed writes never mutate acknowledged in-memory state. Tests cover lock ownership, restart, duplicate observation, wrong job transitions, identity isolation, history saturation, corrupt state and write failures. fmt/check/Clippy/default suite pass (91 passed, one optional Redis test ignored). This is a durable state primitive; deployment worker/config, retry windows, output reconciliation and production acceptance remain pending.

- Wired optional per-region asset_dispatch into deployment preparation and executable background-worker lifecycle. A fresh successful Version observation records destination/profile/catalog identities; a bounded reconciliation cycle submits pending jobs, persists acknowledgements, polls after restart, and accepts completion only when actual catalog identity and requested full/export/publication scope match. Snapshot routes never directly dispatch. Startup enforces independent updater credentials and region/environment/platform ownership of state. Failed/pruned/malformed/mismatched jobs remain terminal with sanitized logs; ambiguous POST delivery is recorded for operator reconciliation rather than blindly replayed without a remote-retention guarantee. Local real gRPC/HTTP integration verifies one submission, background observation, shutdown/restart polling, completion, mismatch rejection and no duplicate ambiguous replay. fmt/check/Clippy/default suite pass (92 passed, one optional Redis test ignored). Administrative recovery/status, history compaction, notifications, remaining platform groups and yhm01/full release acceptance remain pending.

- Added offline asset-dispatch-status and asset-dispatch-adopt commands for existing state directories. Exclusive ownership prevents edits while workers run; adoption only pins an existing UUID for an ambiguous/unacknowledged submission, never submits or marks completion. Reconciliation now validates the remote idempotency digest even for polled/adopted jobs; unrelated IDs fail explicitly. Tests cover durable adoption, persistence failure, rejected transitions and wrong remote identity without duplicate export. fmt/check/Clippy/default suite pass (93 passed, one optional Redis test ignored), with the extended background identity test rerun after its final assertions. Actual CLI subprocess checks pass for status/adoption/lock/UUID/missing-state behavior. Updated archive smoke includes recovery; a temporary macOS archive using the debug binary passes all smoke checks (not release-candidate or three-platform evidence). Online status/recovery, safe history compaction, notifications and remaining restoration/production/release gates remain pending.

- Fixed dispatch starvation: fixed-prefix selection could keep polling the same 16 nonterminal jobs forever while later work never ran. The outbox now atomically persists a rotating reconciliation cursor before each bounded batch. Restart resumes rotation, terminal records are skipped, and failed cursor persistence returns no work for network execution. A 35-job test leaves every job nonterminal while proving all are visited across batches and restart; it also covers wraparound, terminal skipping, new work and write failure. fmt/check/Clippy/default suite pass (94 passed, one optional Redis test ignored). Remaining restoration, production acceptance and 1.2.0 release requirements are unchanged.

- Restored opt-in stale_while_revalidate_ms (0..300000, default disabled) for memory/Redis public-route caches. Freshness and hard retention expiry are separate; Redis PX includes the stale window and cache identity includes its policy. Stale hits schedule bounded background refreshes with separate reservation/fill locks, normal admission/deadlines/session locking, originating account pinning and scope rechecks; fresh/stale cache reads do not wait on account session locks. Failed refreshes never extend retention, no private/profile/deck routes were added, and ranking account-relative fields remain stripped. Tests cover concurrent stale hits with delayed upstream, single refresh, hard expiry/failure recovery, real Redis retention/PX, and existing account/protocol invalidation. fmt/check/Clippy/default suite pass (96 passed, one optional Redis test ignored); isolated Redis test was explicitly run and passed. Remote routing, publication/registry/notifications and remaining restoration plus production/release gates remain pending.

- Added the executing-node portion of remote routing: opt-in peer_token_env enables a separately authorized POST peer/query contract in single/multi-region deployments. Typed public read operations map to the verified local Sirius RPC allowlist; no arbitrary route/account/credential/mutation fields or forwarding hops. Contract version, region, environment, platform, client version and semantic protocol hash must match. The hash is rechecked under admission/protocol locking before account selection or dispatch; pre-dispatch mismatch has its own error distinct from ambiguous codec failures. Region-scoped peer credentials cannot reuse API/admin/game/CDN/updater tokens. Requests have a 16 KiB bound, responses preserve request identity/UUID and typed sanitized outcomes, and shared-account ranking fields are stripped. Tests execute actual local gRPC success/failure and cover auth separation, unknown fields (including Serde unit-variant strictness), identity/capability boundaries, Global shared-schema isolation, disabled deployment and post-reload refusal before network. fmt/check/all-feature Clippy/default tests pass (101 passed, one optional Redis test ignored). This completes the local-only peer contract, NOT the remote routing group: outbound selection/priorities/health/deadline failover/remote-only public integration, registry/publication and remaining restoration plus yhm01/full release acceptance remain pending.

- Added one-attempt peer_transport::Client against the local-only peer contract. It scopes Bearer credentials to an explicit origin/region path, keeps certificate validation, requires explicit private HTTP opt-in, and disables redirects, ambient proxies and reqwest retries. Validates typed requests and exact echoed UUID/identity, JSON outcomes and failure gRPC statuses; preserves int64 strings. Enforces bounded connect/request policies, a caller-owned absolute deadline across headers/body, and both declared/chunked response size limits. Distinguishes proven pre-transmission failures from ambiguous timeout/body/status/protocol failures without automatically replaying. Tightened peer failure enums so unknown fields cannot be silently accepted. Tests cover actual HTTP-peer-to-gRPC execution, both endpoint layouts, wrong region/token, malformed/unbound replies, oversized streams, redirects, untrusted TLS, header/body stalls under both deadlines and mid-body disconnect. fmt/check/all-feature Clippy/default suite pass (106 passed, one optional Redis test ignored). This is the outbound transport foundation, not configured service routing: priorities, passive health, total-budget target selection, remote-only/public integration and remaining registry/restoration/yhm01/release gates remain pending.
