# Sirius 1.2.0 restoration and acceptance ledger

Objective: retain Haruki's reusable service/platform capabilities, replace all Sekai-specific logic with Sirius implementations, test the complete production pipeline on yhm01, then publish both repositories as 1.2.0. A working CLI or one successful export alone is not completion.

Reference baseline: local Haruki-Sekai-API 07da6b80e6a59ece89251f4694afe94bea72e131 and Haruki-Sekai-Asset-Updater 3d33ed037f0ef5009e361e0535b3b19f8c239947. Preserve MIT attribution. Existing Sirius release baselines: API 5ab1e390ca514e550cb609ae4d299b19c105b79e, updater e7ffb7b905958de88aa8d5a06327dafcc8acd8ce.

## API requirements (pending unless evidenced in the work log)

- Multi-region service configuration/routing with per-region protocol family, credentials and isolated state; retain v1.1 single-region config compatibility and reserved CN.
- Account pool, per-account locking, selection, health/cooldown and credential reload. No speculative retries of account mutations; no fabricated Global login.
- Configurable response cache, optional Redis backend, region/account/protocol-aware identity and invalidation; private data never shared through public cache.
- Upstream proxy, deadlines/limits, controlled retry policy, server TLS, logs/access logs and trusted-forwarding configuration.
- Remote node routing, priorities/failover, token-scoped internal calls and capability/version boundaries.
- Master registry/manifests, owner/consumer synchronization, notifications, optional generic persistence and Git publication. Do not restore Sekai table models or Ent code.
- Full documented config surface, examples, migration, meaningful local integration tests including failure/authorization cases.

## Updater requirements (all pending unless specifically evidenced)

- Authenticated long-running HTTP service, region configs, job submission/list/detail/cancel/retry, bounded queue/concurrency, progress, retention and safe shutdown/restart.
- Invoke Sirius catalog/download/verify/export pipeline from the service, not a stub or arbitrary shell executor; retain CLI support.
- Incremental download/export records by verified content identity; cache reuse, partial failure recovery and immutable publication semantics.
- Include/exclude filters, priority ordering, exact type selection and scoped replay; selected work is never claimed as full-catalog verification.
- OpenDAL/local/S3 providers, upload concurrency/retries and verified upload-before-cleanup policy; region-scoped object keys and secret references.
- Format configuration for image/audio/video and backend choice where implementable for Sirius; retain correct alpha/split audio semantics.
- Separate stage concurrency, bounded resource budgets, configurable retries/timeouts, logging/access logging and TLS/proxy configuration.
- Update trigger/scheduling, completion notification and generic publication integration where applicable. Sekai chart-hash/character-ID/3D model adapters are removed, not copied.
- End-to-end tests cover queue saturation, cancellation, restart, per-region serialization, incremental behavior, upload failure safety and secrets.

## Release gates (all pending)

- Audit each feature against original code/config; no placeholders or silent ignored config fields; record any game-specific non-applicability with evidence.
- Both repositories fmt/check/Clippy/tests and release packaging smoke pass.
- yhm01 official candidate full JP download/decrypt/export through restored service; retain and independently hash every exported file.
- yhm01 verify incremental second run, job API/auth/cancel, resource limits, restart and configured storage path; existing Haruki services unchanged.
- Global remains limited to verified contracts unless additional protocol research validates functionality; region isolation test for TW/EN/KR and CN rejection.
- Public source/artifact secret audit, three-platform artifacts, exact commits, version 1.2.0 tags/releases, published artifact audit.

## Work log

- Initial audit confirmed the missing components are real scope loss, not merely shorter examples/defaults.
- Implementation begins with the updater's durable job lifecycle and service orchestration; API restoration follows against the same ledger.

- Updater durable job ledger implemented and five lifecycle tests passed: restart interruption, exclusive state ownership, queue/retention bounds, per-region scheduling, cancellation acknowledgement, storage failure atomicity and reserved-region rejection. HTTP and pipeline integration remain pending; this does not satisfy the service gate yet.

- User requires category investigation before adapting filters. Verified Sirius catalog labels: InitialDownload, Everything, MV; use native key/dependency membership, not Sekai start_app/on_demand. Everything is a subset of full catalog (JP baseline: 13,364 versus 13,367 remote files). Exact tutorial/runtime required-address composition remains unverified; no invented preset.

- Multi-region service assembly implemented with one listener, independent regional clients/protocols/observations/locks, legacy single-region routes and per-region bearer scopes. Local four-region tests verify wrong-scope rejection, Global 501, CN/unknown route rejection and JP-only protocol reload. Updater opt-in regional routes cover version refresh and snapshot transport. Account pools, cache and the remaining API requirements are still pending.

- Account pooling restored: least-loaded selection with rotating ties, per-account session locks, stable private identity routes, transient cooldown, auth-failure quarantine and atomic credential-file reload after active calls drain. Legacy account configuration is supported. Local HTTP/2 fixtures cover concurrent independent accounts, same-account serialization, non-replay on failed auth, timeout recovery, credential activation barriers and invalid-file rollback. No Global login or registration was fabricated.

- Regional gRPC request policy now configures one end-to-end deadline, admission concurrency, wire response size and bounded anonymous-read retries. Local HTTP/2 tests verify shrinking timeout headers, exact retry count, maintenance/authenticated non-replay, queued-request deadline accounting, permit release and valid oversized protobuf rejection. Upstream proxy, server TLS, access logging and forwarding trust remain pending.

- Added bounded memory and optional Redis caching for public announcements and sanitized rankings only. Profile/deck/identity/private-data/version calls bypass caching. Keys scope region, origin, account credential digest/generation, protocol fingerprint/generation, Master version and request input; cache errors fail open within operation/request budgets. Local tests cover TTL/bounds, error exclusion, reload invalidation and isolated Redis outage/account separation. Per-endpoint TTLs, single-flight and stale-while-revalidate remain pending.

- Cache policy now supports separate announcement/event/song/challenge TTLs, per-route disablement and process-local single-flight fills with bounded lock storage. Effective TTL is included in cache identity and applied to Redis expiry. Tests verify independent expiry, disabled-route RPC behavior, cancellation-safe fill handoff and concurrent miss coalescing. Stale-while-revalidate remains pending.

- Regional outbound HTTP/HTTPS CONNECT proxies now sit beneath the existing verified origin TLS/HTTP2 stack, with separately scoped environment-referenced proxy authorization and a full connection-establishment deadline. Refused/malformed tunnels fail without direct fallback or retry; local fixtures verify RPC/trailer preservation, credential separation, reuse, proxy isolation, deadlines, DNS/IPv6 authorities and rejection of untrusted certificates at both TLS layers. fmt/check/Clippy and the default suite pass (72 passed, one optional Redis test ignored). This completes game gRPC proxy transport only; CDN transport, inbound TLS/access logs/trusted forwarding and other restoration gates remain pending.

- Optional inbound TLS is now wired into the production listener, with bounded PEM reads, certificate/key matching, Unix private-key permissions, ALPN HTTP/1.1+HTTP/2 and a configurable handshake timeout. Certificates load before bind/background workers. Existing plaintext deployment remains supported; TLS closes plaintext/stalled peers and gracefully drains active requests. API multi-region TLS belongs only at the deployment root. fmt/check/Clippy and the default suite pass (76 passed, one optional Redis test ignored). Structured/access logging and forwarding trust remain pending.

- Access logging is now configurable at the listener root: JSON/text, stdout or rotating files, bounded asynchronous queue and retention. Records contain only matched route templates, generated request IDs, peer/client IPs, outcome/status and header-response latency; credentials, query strings, bodies and route IDs are excluded. Trusted forwarding walks explicit CIDRs from the socket peer toward the first untrusted hop and rejects malformed/duplicate/oversized headers. Tests exercise redaction, spoofed chains, IPv6, cancellation, append behavior and HTTPS/HTTP2 listener integration. fmt/check/Clippy and the full default suite pass: 79 passed, one optional Redis test ignored. Application log configuration remains pending.

- Master CDN transport now has its own validated HTTP/HTTPS proxy references, connection/body timeouts, whole-update deadline and bounded transient retries under `master_update.network`. Defaults retain the previous one-attempt/10s-connect/60s-request/600s-update policy; waiting for the updater lock now consumes the same deadline. Explicit proxy authentication remains separate from CDN Basic credentials and game RPC sessions, with normal TLS verification, no redirects, no direct fallback and no ambient proxy discovery. Tests cover actual manifest/table publication through a proxy, HTTP retry counts, terminal auth/redirect/integrity failures, request timeout recovery, whole-deadline/backoff/queue bounds, invalid config before I/O and both proxy/origin TLS rejection. fmt/check/Clippy/default tests pass (86 passed, one optional Redis test ignored). Master registry/owner-consumer sync, generic persistence/publication/notifications and other platform/production gates remain pending.

- Application logging is now configured at the executable/service root: off/error/warn/info/debug/trace, text/JSON, stderr/stdout or UTC-rotating append files, bounded queue and retention. It reuses the access-log sink but filters to application targets and an explicit operational-field allowlist; dependency request payloads and raw resource/decoder details are excluded. Updater stages/retries/progress and persisted job terminal states emit structured events. Default events use stderr, preserving CLI stdout; existing CLI diagnostics remain separate. Multi-region/per-profile nested logging is rejected. Tests verify levels, forbidden fields/dependency targets, numeric JSON fields, escaped newlines, UTF-8 limits, append/flush, disabled logging and invalid sinks. fmt/check/Clippy/default tests pass (88 passed, one optional Redis test ignored). Both actual local service processes also passed JSON file output, clean stdout and SIGTERM exit/flush checks; the updater check included a persisted failed job and its terminal log event. Other restoration groups and full yhm01/three-platform release acceptance remain pending.

- Added API-side typed asset job HTTP transport for submit/poll: dedicated bearer origin, optional explicit plaintext, normal TLS verification, no ambient proxy/redirect/fallback/retry, bounded deadlines and 64 KiB streamed response cap. Validates request/job/key identity, completed outcome region/digest and publication shape; errors never expose upstream bodies or tokens. Local HTTP tests cover accepted/polled/completed jobs, wrong identities/digests, malformed/oversized results, terminal-state consistency, 302/401/404/409/429/503, timeout, reserved CN and unsafe config. fmt/check/Clippy/default suite pass (89 passed, one optional Redis test ignored). docs/ASSET_DISPATCH.md documents responsibility split and the still-pending durable owner/config/worker integration; no inert config setting is exposed. Full production/release gates remain pending.

- Added the API dispatch outbox primitive: single-owner atomic ledger, deterministic region/destination/profile-revision/catalog/scope identity, pending-before-send and first-attempt persistence, pinned acknowledged job IDs, terminal completion/failure records and explicit bounded history without silent pruning. Process restart preserves ambiguous sends and known completed/failed identities; failed writes never mutate acknowledged in-memory state. Tests cover lock ownership, restart, duplicate observation, wrong job transitions, identity isolation, history saturation, corrupt state and write failures. fmt/check/Clippy/default suite pass (91 passed, one optional Redis test ignored). This is a durable state primitive; deployment worker/config, retry windows, output reconciliation and production acceptance remain pending.

- Wired optional per-region asset_dispatch into deployment preparation and executable background-worker lifecycle. A fresh successful Version observation records destination/profile/catalog identities; a bounded reconciliation cycle submits pending jobs, persists acknowledgements, polls after restart, and accepts completion only when actual catalog identity and requested full/export/publication scope match. Snapshot routes never directly dispatch. Startup enforces independent updater credentials and region/environment/platform ownership of state. Failed/pruned/malformed/mismatched jobs remain terminal with sanitized logs; ambiguous POST delivery is recorded for operator reconciliation rather than blindly replayed without a remote-retention guarantee. Local real gRPC/HTTP integration verifies one submission, background observation, shutdown/restart polling, completion, mismatch rejection and no duplicate ambiguous replay. fmt/check/Clippy/default suite pass (92 passed, one optional Redis test ignored). Administrative recovery/status, history compaction, notifications, remaining platform groups and yhm01/full release acceptance remain pending.

- Added offline asset-dispatch-status and asset-dispatch-adopt commands for existing state directories. Exclusive ownership prevents edits while workers run; adoption only pins an existing UUID for an ambiguous/unacknowledged submission, never submits or marks completion. Reconciliation now validates the remote idempotency digest even for polled/adopted jobs; unrelated IDs fail explicitly. Tests cover durable adoption, persistence failure, rejected transitions and wrong remote identity without duplicate export. fmt/check/Clippy/default suite pass (93 passed, one optional Redis test ignored), with the extended background identity test rerun after its final assertions. Actual CLI subprocess checks pass for status/adoption/lock/UUID/missing-state behavior. Updated archive smoke includes recovery; a temporary macOS archive using the debug binary passes all smoke checks (not release-candidate or three-platform evidence). Online status/recovery, safe history compaction, notifications and remaining restoration/production/release gates remain pending.

- Fixed dispatch starvation: fixed-prefix selection could keep polling the same 16 nonterminal jobs forever while later work never ran. The outbox now atomically persists a rotating reconciliation cursor before each bounded batch. Restart resumes rotation, terminal records are skipped, and failed cursor persistence returns no work for network execution. A 35-job test leaves every job nonterminal while proving all are visited across batches and restart; it also covers wraparound, terminal skipping, new work and write failure. fmt/check/Clippy/default suite pass (94 passed, one optional Redis test ignored). Remaining restoration, production acceptance and 1.2.0 release requirements are unchanged.

- Restored opt-in stale_while_revalidate_ms (0..300000, default disabled) for memory/Redis public-route caches. Freshness and hard retention expiry are separate; Redis PX includes the stale window and cache identity includes its policy. Stale hits schedule bounded background refreshes with separate reservation/fill locks, normal admission/deadlines/session locking, originating account pinning and scope rechecks; fresh/stale cache reads do not wait on account session locks. Failed refreshes never extend retention, no private/profile/deck routes were added, and ranking account-relative fields remain stripped. Tests cover concurrent stale hits with delayed upstream, single refresh, hard expiry/failure recovery, real Redis retention/PX, and existing account/protocol invalidation. fmt/check/Clippy/default suite pass (96 passed, one optional Redis test ignored); isolated Redis test was explicitly run and passed. Remote routing, publication/registry/notifications and remaining restoration plus production/release gates remain pending.

- Added the executing-node portion of remote routing: opt-in peer_token_env enables a separately authorized POST peer/query contract in single/multi-region deployments. Typed public read operations map to the verified local Sirius RPC allowlist; no arbitrary route/account/credential/mutation fields or forwarding hops. Contract version, region, environment, platform, client version and semantic protocol hash must match. The hash is rechecked under admission/protocol locking before account selection or dispatch; pre-dispatch mismatch has its own error distinct from ambiguous codec failures. Region-scoped peer credentials cannot reuse API/admin/game/CDN/updater tokens. Requests have a 16 KiB bound, responses preserve request identity/UUID and typed sanitized outcomes, and shared-account ranking fields are stripped. Tests execute actual local gRPC success/failure and cover auth separation, unknown fields (including Serde unit-variant strictness), identity/capability boundaries, Global shared-schema isolation, disabled deployment and post-reload refusal before network. fmt/check/all-feature Clippy/default tests pass (101 passed, one optional Redis test ignored). This completes the local-only peer contract, NOT the remote routing group: outbound selection/priorities/health/deadline failover/remote-only public integration, registry/publication and remaining restoration plus yhm01/full release acceptance remain pending.

- Added one-attempt peer_transport::Client against the local-only peer contract. It scopes Bearer credentials to an explicit origin/region path, keeps certificate validation, requires explicit private HTTP opt-in, and disables redirects, ambient proxies and reqwest retries. Validates typed requests and exact echoed UUID/identity, JSON outcomes and failure gRPC statuses; preserves int64 strings. Enforces bounded connect/request policies, a caller-owned absolute deadline across headers/body, and both declared/chunked response size limits. Distinguishes proven pre-transmission failures from ambiguous timeout/body/status/protocol failures without automatically replaying. Tightened peer failure enums so unknown fields cannot be silently accepted. Tests cover actual HTTP-peer-to-gRPC execution, both endpoint layouts, wrong region/token, malformed/unbound replies, oversized streams, redirects, untrusted TLS, header/body stalls under both deadlines and mid-body disconnect. fmt/check/all-feature Clippy/default suite pass (106 passed, one optional Redis test ignored). This is the outbound transport foundation, not configured service routing: priorities, passive health, total-budget target selection, remote-only/public integration and remaining registry/restoration/yhm01/release gates remain pending.

- Integrated node_routing into all existing public game query handlers, with configurable local/remote priorities, remote-only public frontends, bounded admission, one overall deadline, passive failure cooldown and one half-open probe. Ties prefer local, then configured remote order. Authenticated queries only fail over after proven pre-dispatch rejection/connection failure; ambiguous delivery remains terminal, and game gRPC outcomes never trigger speculative failover. Added a distinct account-admission failure before game dispatch and executor observations in peer replies. Public system status uses the selected executor's observation without altering local account/CDN/resource state; administrative/account/Master/snapshot/dispatch operations and incoming peer calls remain local. Administrative nodes status exposes names/health without URLs or credentials; deployment rejects outgoing token reuse with other privilege scopes or regions. Tests exercise real remote HTTP-to-gRPC system/profile queries, account admission fallback, preferred/tied priorities, no forwarding despite a preferred remote, cooldown/single-probe recovery, authenticated non-replay, terminal game errors, shared deadline, cancellation/admission release, credential scope and remote ranking field stripping. fmt/check/all-feature Clippy/default suite pass (115 passed, one optional Redis test ignored). Public query routing is now active configuration, not a library stub. Real yhm01 node/failure acceptance, Master registry/consumer/publication/notifications, remaining updater restoration and full candidate/release gates remain pending.

- Added the Master producer/read protocol over existing atomic snapshots. Every new local/CDN import stages a sorted tables.json plaintext SHA-256/size index before CURRENT changes; ordinary indexed table reads reject content corruption. Authenticated current/pinned manifests include region/environment/platform, source version/metadata and deterministic content identity; digest-qualified pinned table URLs preserve exact JSON bytes, private caching and conditional ETags. CURRENT is read once, old snapshots stay independently addressable, and registry reads never call the game. Validated source receipts, safe listed paths, source/index membership and size/hash bounds; reject linked snapshot/files and the MasterManifest.bin table name that would overwrite source metadata. Legacy 1.1 snapshots without an index compute manifests in memory, preserving read compatibility without disk mutations; malformed existing indexes fail closed. Tests cover CURRENT rollover, same-content identity across new UUIDs, scope changes, independent Python SHA/canonical-JSON vectors, corruption, legacy behavior, symlinks, HTTP authorization/304 and exact pinned bytes. fmt/check/all-feature Clippy/default suite pass (119 passed, one optional Redis test ignored). This is the actual producer endpoint and integrity foundation; consumer synchronization, registry history/persistence, notifications, Git publication, remaining updater/platform restoration and yhm01/full release gates remain pending.

- Added configured JP Master consumer synchronization and the master-sync one-shot command. Consumers pin scoped owner manifests, verify content identity and every table, reuse only verified local bytes, stage all files, recheck owner content, then atomically publish their own snapshot. Same-content owner UUID rollover is accepted; changed content, corruption, cancellation and timeout preserve the installed pointer. Background preparation owns staging/writer and cannot publish after cancellation. Service assembly separates owner public-read credentials from administrative/peer/game/CDN/updater scopes, excludes simultaneous CDN updating, and starts/stops consumer workers. Bounded JSON requests use verified TLS, no redirects/ambient proxies/automatic retries; failed attempts retry on the next configured poll. Documented defaults, bounds, retained history and synchronous filesystem deadline limits. Five integration tests cover real producer HTTP reads, corruption repair, version-only byte reuse, scope mismatch, owner rollover, shutdown, writer recovery, deadline, config and deployment assembly. fmt/check/all-feature Clippy/default suite pass (124 passed, one optional Redis test ignored). Registry history/persistence, completion notifications, Git publication, remaining updater/platform restoration and yhm01/full candidate release gates remain pending.

- Restored local committed Master installation history across import/CDN/consumer publication. Each staged snapshot now carries a synced publication.json predecessor/time record before CURRENT is switched; history pins CURRENT and follows only committed links rather than scanning directories, excluding orphan snapshots. Added bearer-authenticated GET master-data/history with default 20/max 100 entries, scope/content identity/file totals, has_more and explicit legacy_boundary. Duplicate explicit reimports remain installation records with identical content hashes; unchanged updater polls add nothing. Corrupt/cyclic/unsafe/linked records fail, and an invalid existing pointer cannot silently reset history. Legacy snapshots stop traversal with unknown time instead of fabricated mtime ordering. Tests cover version changes, reimports, orphan exclusion, restart-compatible disk reads, bounds, legacy boundary, unsafe pointers, corrupt/cyclic/linked history and authorized local-only HTTP reads. fmt/check/all-feature Clippy/default suite pass (127 passed, one optional Redis test ignored). Full-history pagination, external registry database persistence, notifications/Git publication, remaining restoration and yhm01 candidate/full release gates remain pending.

- Added Master history pagination with optional before and returned next_before. Cursors must be safe bounded snapshot names reachable from the pinned CURRENT predecessor chain; orphan/missing/unreachable cursors return 404, malformed cursors 400. New publications between requests do not reorder older results; repeat cursors are stable, oldest cursors yield empty final pages, and legacy boundaries remain explicit. Traversal is bounded to 10,000 links per request and limit exhaustion fails rather than silently truncating; deep indexing/retention remain pending. Tests import 105 real synthetic snapshots and read all three pages without gaps/duplicates, plus authenticated HTTP concurrent publication, replay, orphan, invalid-query and legacy-boundary cases. fmt/check/all-feature Clippy/default suite pass (129 passed, one optional Redis test ignored). Central persistence/notifications/Git publication and final production/release gates remain open.

- Restored authenticated consumer notification receiver POST /internal/v1[/region]/master-data/sync with strict 4 KiB scope/hash hints. It coalesces to one pending in-memory wakeup on the existing single sync worker, including hints before its wait, and keeps periodic/immediate-startup polling. Responses acknowledge wakeup only; configured owner manifests remain authoritative, no caller origin/path/content is accepted. HTTP/integration test verifies public-token rejection, wrong scope/hash, unknown origin, oversized body, disabled consumer, burst coalescing and actual verified version update within five seconds despite a one-day polling interval and stale hint digest. fmt/check/all-feature Clippy/default tests pass (130 passed, one optional Redis ignored). Owner-side delivery/retry, central persistence/Git/publication and final production/release gates remain pending.

- Added bounded Master owner notification transport with strict explicit-origin/token validation, fixed scoped receiver paths, verified TLS, disabled redirects/proxies/implicit retries, a whole-request timeout and 1 KiB replies. Only strict HTTP 202 accepted responses advance per-target content acknowledgement; failures remain retryable, newer content can supersede failed hints, and restart can resend current content. Real HTTP tests cover request scope/auth, failures, deduplication/restart, origin rejection, redirects, oversized/unknown-field replies and stalled body deadlines. fmt/check/all-feature Clippy/default tests pass (132 passed, one optional Redis ignored). This is transport groundwork only: deployment configuration, committed-CURRENT reconciliation, retry scheduling and cross-scope outgoing credential checks remain pending, alongside the existing restoration/production/release gates.

- Integrated opt-in JP master_notify into configuration, deployment preparation and service shutdown. Bounded named targets use remote consumer internal tokens distinct from all configured local API/internal/peer/game/CDN/owner-read/updater/node/proxy credentials. Workers reconcile committed CURRENT on startup, successful in-process publication and configured intervals; independent CLI imports are picked up by polling. Failed targets retry without rolling back CURRENT or blocking later targets; accepted targets deduplicate unchanged content, restart resends current content, intermediate versions may coalesce. Tests exercise real owner-to-consumer verified synchronization with one-day consumer polling, actual ten-second failure retry, healthy-target deduplication, restart reconciliation, config/cross-profile credential rejection and cancellation of a stalled thirty-second request. fmt/check/all-feature Clippy/default suite pass (136 passed, one optional Redis ignored). Generic completion webhooks, central registry persistence/Git publication, updater restoration and final production/release gates remain pending.

- Restored online asset-dispatch administration: internal-only regional/single-profile paginated entries and existing-job adoption endpoints share the worker's single durable state owner via a bounded command queue. Queries default to 50/max 200 entries; adoption accepts only a canonical existing job UUID and existing safe transitions, never POSTs to the updater or claims completion. Network reconciliation and recovery cannot race; saturated/stopped/busy workers return 503 within five seconds, and requests abandoned while queued are discarded. Tests cover live worker status without duplicate submission, authorization, pagination, invalid/unknown fields, conflict/idempotent adoption, durable reopen, timed-out adoption nonexecution and stopped-owner status. fmt/check/all-feature Clippy/default tests pass (138 passed, one optional Redis ignored). History compaction, central persistence/Git publication, updater restoration and final full candidate/production/release gates remain pending.

- Restored content-addressed Master manifest lookup under /master-data/by-hash/{content_sha256}/manifest. Resolves the newest matching installation only through the pinned committed history, scopes identity to region/environment/platform, excludes orphan directories and stops at legacy boundaries. Reuses history-link validation, bounds traversal to 10,000 and fails on corrupt state; strict hashes/auth/ETag behavior are covered. Reimports may change local snapshot UUID/response ETag, so responses remain private/no-cache. Local/HTTP regression covers old-version lookup, identical reimports, orphan exclusion, cross-scope misses, auth, malformed hashes, conditional 304, legacy boundaries and corruption. fmt/check/all-feature Clippy/default tests pass (139 passed, one optional Redis ignored). This is local content-hash lookup, not completion of central database persistence, deep indexing, Git publication or remaining updater/production/release gates.

- Added bounded Git execution groundwork for optional Master publication. Commands run without a shell, with disabled terminal prompts, concurrent bounded stdout/stderr reads and static errors that cannot echo credentials. Unix cancellation/error kills the owned process group, including transport helpers; explicit timeout cleanup bounds direct-child reaping. Real Git initialization/read tests and actual delayed child-side-effect tests cover successful execution, errors without raw stderr leakage, stdout/stderr floods, invalid policies, missing executables, timeout and future cancellation with a positive control. fmt/check/all-feature Clippy/default tests pass (141 passed, one optional Redis ignored). Only the existing locked libc version became a direct Unix dependency. Git snapshot staging/push/recovery/configuration, Windows process containment, central persistence and remaining restoration/production/release gates are still pending; this primitive is not automatically invoked by the service.

- Added actual local Master Git publication and master-git-commit CLI. Pins and verifies every source table before Git writes, stages exact blobs without filters, omits node-local UUIDs from publication metadata, creates fresh trees (including table removals), deduplicates identical trees, and commits with a parent and compare-and-swap branch reference. Dedicated scoped state ownership and file locking reject other repositories/scopes/symlinks/concurrent writers. Extended the bounded Git runner with bounded stdin and removal of ambient GIT_* redirects/config/traces. Real Git tests verify blobs, metadata, history/deduplication, corruption preserving refs/CURRENT and ownership rejection. Required checks pass (143 passed, one optional Redis ignored); an actual built CLI synthetic import/commit/add-table/remove-table flow verifies exactly three commits and identical-content reuse. This is local publication only: remote push/remote-ahead/retry, configured signing/identity/proxy/credentials, background integration and Windows containment remain pending along with central persistence and final restoration/production/release gates.

- Added explicit Master Git remote publication and master-git-push CLI. Under the same source verification/state lock, inspect remote history before local commit creation, reject ahead/diverged/unverifiable refs, fetch only to a private local inspection ref, perform a non-force push and verify the remote ref before acknowledging publication. Failed pushes preserve/reuse the local commit. HTTPS authorization uses a scoped environment-referenced header via Git config-env, with verified TLS, no redirects/ambient helpers/proxies/system/global Git config; file URLs support explicit local mirrors/tests and cannot carry authorization. Local bare-remote regression verifies rejected-push recovery without duplicate commits, remote-ahead refusal without branch changes and compatible behind-remote advancement. Real HTTP fixture verifies scoped header delivery and no redirect follow. A built CLI synthetic import/push/repeat flow also verifies the exact remote ref and same-commit reuse against a temporary local bare repository. fmt/check/all-feature Clippy/default tests pass (145 passed, one optional Redis ignored). Service configuration/background policy, signing/author/proxy options, Windows containment, central persistence and final production/release gates remain pending.

- Integrated opt-in JP Master Git publication into service configuration, deployment preparation and worker shutdown. Startup/current reconciliation, an independent successful-install wake signal and bounded periodic retries invoke the verified local/remote publisher; rejected pushes retain installed Master and pending Git commits. Internal regional/single-profile status exposes only receipt/static error state, with last success retained through failures. Deployment-wide credential checks compare underlying Bearer tokens and decoded Basic passwords against other service scopes and across profiles. Actual local bare-remote worker tests cover startup/update, independent consumer notification, rejected-push periodic recovery without duplicate commits, authenticated/redacted status, stalled-network cancellation and lock release on shutdown. Config tests cover missing source, interval bounds, malformed/reused/cross-profile credentials. Required fmt/check/all-feature Clippy/default tests pass (149 passed, one optional Redis ignored). Signing/author/proxy policies, Windows containment, central persistence, remaining updater restoration and final exact-candidate production/release gates remain pending.

- Restored configured Master Git author/committer identity and OpenPGP/SSH signatures for service workers and both Git CLI commands. Explicit signing is applied to new commit-tree objects; identical trees retain their original commits/history, including previously unsigned commits. Configuration rejects invalid identities, private-key text as OpenPGP fingerprint, relative SSH key paths and shell expressions in custom signer programs. Signing errors retain the previous Git ref and installed CURRENT; signer descendants remain under the existing command deadline/process group. Actual temporary SSH and isolated OpenPGP keyring tests independently verify signatures; SSH failure/recovery preserves refs and history. Built CLI synthetic import/configured-author/signed-commit/independent-verify/repeat flow also passes. Required checks pass151/default2ignored; optional OpenPGP test separately executed and passed (Redis remains unrun). Proxy configuration, Windows containment, central persistence, remaining updater restoration and final exact-candidate production/release gates remain pending.

- Restored independent environment-referenced Git proxy configuration for service remotes and explicit CLI pushes. HTTP/HTTPS/SOCKS5h proxy URLs are validated, optional credentials stay out of argv/config/errors, origin and HTTPS-proxy certificate verification remain enabled, and ambient proxy/bypass variables are removed from Git subprocesses. File remotes reject proxies; omitted proxy forces direct transport. Real local tests cover HTTP absolute-URI routing with no direct fallback, CONNECT 407 authentication without origin Authorization leakage and SOCKS5h remote DNS. Tests also pass with both NO_PROXY and no_proxy set to wildcard. Required fmt/check/all-feature Clippy/default suite pass155/2ignored; optional isolated OpenPGP regression separately passes. Windows containment, central persistence, updater restoration and exact final candidate/production/release gates remain pending.

- Connected optional asset_dispatch.targets[].user_agent to the dedicated updater transport. Bounded printable ASCII is validated at configuration/startup; submission and polling share the explicit header alongside the separately scoped Bearer token. Actual HTTP fixtures validate both operations and worker restart/reconciliation; invalid values fail before network I/O. Header changes do not enter durable catalog/job identity. Required fmt/check/all-feature Clippy/default tests pass155/2optionalignored. This supports the updater's optional client-prefix filter; remaining registry/config/platform and final exact-candidate production/release gates remain open.

- Added explicit completed-dispatch compaction: archive terminal receipts before removing them from the capacity-limited working ledger, retain identity checks on every future observation, and support idempotent recovery when only the archive write completed. Online authenticated archive/detail commands use the sole-owner queue; offline archive/entry CLI commands use the existing exclusive lock. Noncompleted/ambiguous/failed entries cannot be archived. Tests verify capacity recovery, cursor reset, persistence failure/restart, corrupt history rejection, authorization and actual worker no-resubmission after archival. Required fmt/check/all-feature Clippy/default tests pass157/2optionalignored; the built local CLI passes archive/status/entry/idempotency/refused-adoption smoke. Archives remain on disk and must accompany backups; no automatic history deletion or final platform/production acceptance is claimed. Remaining registry/config/platform and exact final-candidate production/release gates stay open.

- Added explicit master-db-import with a PostgreSQL mirror of verified manifests, exact JSON bytes and indexed JSONB documents. A single transaction covers scoped current/history and retention; existing content is reverified, repeated current imports deduplicate, and failed/cancelled work preserves the database current pointer and local files. SQLx transport uses verified TLS by default, explicit bounded connection policy, environment-referenced passwords, no statement logging and rejection of ambient PG options. Actual isolated PostgreSQL tests prove insertion-failure rollback, timeout/cancellation, concurrent deduplication, scoped retention, corruption refusal and plaintext-server TLS rejection; actual built CLI verifies synthetic import/repeat and oversized-config refusal. Required fmt/check/all-feature Clippy/default tests pass158/3optionalignored, with the new PostgreSQL optional test separately passing. This restores an explicit importer, not background integration, database HTTP reads, file-state migration or standalone registry service; those and final platform/candidate/release gates remain open.

- Integrated optional master_database background publication into JP single/multi-profile service configuration, startup, independent Master-install wake, periodic retry and shutdown. Internal database status preserves last success and exposes only static errors; database passwords are checked against all configured service credential scopes (including underlying authorization values). Real isolated PostgreSQL tests cover startup, notification independence, timed failed-insert recovery, authorization/redaction, cancellation while a transaction waits, and restart reconciliation/deduplication. Repeated-test fixtures now use unique update versions so existing content cannot bypass deliberate insert failure. Full regression exposed inherited-descriptor lock lifetimes in existing Master/outbox/Git ownership; all use an explicit-unlock guard, with deterministic duplicate-description exclusion/reacquisition coverage. Required fmt/check/all-feature Clippy/default tests pass160/4optionalignored. Database HTTP reads/committed file-history migration/standalone registry and final platform/candidate/production/release gates remain pending.
